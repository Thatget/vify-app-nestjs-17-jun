<script>
    {% assign current_variant = product.selected_or_first_available_variant %}
    var vifyRequestFQ = {};
    vifyRequestFQ.lineItem = {{ product | json }}
    var jsVariants = [];
    {% for variant in product.variants %}
    jsVariants.push({
        ...{{ variant | json }},
        price_formatted: `{{ variant.price | money }}`
    })
    {% endfor %}
    if (vifyRequestFQ) {
        vifyRequestFQ.lineItem.variants = jsVariants;
        window.vifyRequestFQ = vifyRequestFQ;
    }

    var jsProduct = {};
    jsProduct = vifyRequestFQ
    var variant_selected_id = '';
    variant_selected_id = {{ current_variant.id }}

</script>
{% assign jsProducts = "test" %}

<script>
    jsProducts = "new Test"
</script>
<div>The value of : {{ jsProducts }}</div>

<!-- Product price -->
<div class="price">
    <span class="price-reg">{{ current_variant.price | money }}</span>

    {% if current_variant.compare_at_price > current_variant.price %}
        <span class="price-sale"><s>{{ current_variant.compare_at_price | money }}</s></span>
    {% endif %}
</div>

<div>v 1.4</div>
<div id="vify_rfq-f">b</div>
{% schema %}
{
  "name": "Vify Quote",
  "target": "section",
  "stylesheet": "index.css",
  "javascript": "index.js",
  "settings": []
}
{% endschema %}

